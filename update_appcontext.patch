--- lib/context/AppContext.tsx
+++ lib/context/AppContext.tsx
@@ -404,6 +404,12 @@
 
 const AppContext = createContext<AppContextType | undefined>(undefined);
 
+const INITIAL_SECTORS: Sector[] = [
+  { id: 1, name: 'Alimentos', desc: 'Despensa e Geladeira', items: 142, cost: 'R$ 450,00', iconId: 'Utensils', colorId: 'emerald' },
+  { id: 2, name: 'Limpeza', desc: 'Produtos de Limpeza', items: 38, cost: 'R$ 120,50', iconId: 'SprayCan', colorId: 'blue' },
+  { id: 3, name: 'Eletrônicos', desc: 'Dispositivos e Cabos', items: 65, cost: 'R$ 1.250,00', iconId: 'Laptop', colorId: 'purple' }
+];
+
 export function AppProvider({ children }: { children: React.ReactNode }) {
   const [theme, setThemeState] = useState<ThemeMode>(() => {
     if (typeof window !== 'undefined') {
@@ -420,6 +426,7 @@
   const [shoppingItems, setShoppingItems] = useState<ShoppingItem[]>(INITIAL_SHOPPING_ITEMS);
   const [priorityItems, setPriorityItems] = useState<PriorityItem[]>(INITIAL_PRIORITY_ITEMS);
   const [maintenances, setMaintenances] = useState<MaintenanceItem[]>(INITIAL_MAINTENANCES);
+  const [sectors, setSectors] = useState<Sector[]>(INITIAL_SECTORS);
 
   // Setup default theme based on system preference if local storage is empty
   useEffect(() => {
